# App
HOST=0.0.0.0
PORT=3001
NODE_ENV=production

# Database
# Local (yarn dev + docker-compose.dev.yml): DB_HOST=localhost
# Docker full stack (docker-compose.yml):     DB_HOST=postgres  ← compose tự override
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=change-me
DB_NAME=mecha
DB=postgres

JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d
JWT_EXPIRES_REFRESH_IN=20d
JWT_SALT_ROUNDS=10

SWAGGER_TITLE=API Documentation
SWAGGER_DESCRIPTION=NestJS Swagger Documentation
SWAGGER_VERSION=1.0

UPLOAD_DIR=storages/uploads

SITE_URL=https://www.thegioivandien.com
